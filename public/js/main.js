// Generated by CoffeeScript 1.6.3
(function() {
  var exports, header;

  exports = window;

  exports.init = function() {
    return header();
  };

  window.placeholder = function() {
    var form, password, username;
    form = $('form[name=login]');
    username = form.find("input[name='LoginForm[username]']");
    password = form.find("input[name='LoginForm[password]']");
    if (username.val() === '') {
      username.parent().find('label').show();
    }
    if (password.val() === '') {
      password.parent().find('label').show();
    }
    form.find('label[data-placeholder=true]').click(function() {
      return $(this).parent().find('input').focus();
    });
    return form.find("input[name='LoginForm[username]'], input[name='LoginForm[password]']").focus(function() {
      $(this).parent().find('label').hide();
      return $(this).parent().find('.errorMessage').remove();
    }).blur(function() {
      var el;
      el = $(this);
      if (el.val() === '') {
        return el.parent().find('label').show();
      }
    });
  };

  header = function() {
    return $('#header').animate({
      top: 0
    });
  };

}).call(this);
